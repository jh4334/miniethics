# 🎨 에셋 교체 가이드

지금 앱은 모든 그림을 **이모지 플레이스홀더**로 표시합니다.
GPT 등으로 생성한 귀여운 이미지로 바꾸는 방법을 정리했어요.

## 1. 어디를 바꾸면 되나요?

### 방법 A: 중앙 에셋 맵 (공용 캐릭터)

`js/data/assets.js`의 값을 이미지 경로로 바꾸는 방식입니다.
> 참고: 이 에셋 맵은 아직 화면 코드에 연결되지 않은 자리표시자입니다. 이미지가 준비되면 연결 작업(각 게임의 이모지 → 이미지 교체)을 함께 진행합니다. 자세한 프롬프트와 파일명 규약은 [docs/GPT_ASSET_PROMPTS.md](docs/GPT_ASSET_PROMPTS.md)를 보세요.

```js
// 변경 전
mascot: '🤖',

// 변경 후 (assets/characters/ 폴더에 이미지를 넣고)
mascot: { img: 'assets/characters/mascot.png' },
```

### 방법 B: 각 게임/데이터 파일의 이모지

게임별 아이템은 각 파일 상단의 데이터 배열에 모여 있습니다.
예: `js/games/g01_classify.js`의 `ITEMS`, `js/data/lessons.js`의 `concepts.emoji` 등.
`e: '🐱'` 같은 값을 `<img>` 태그 문자열로 바꾸거나, 이모지 대신 이미지를 쓰도록 요청해 주세요.

## 2. 추천 이미지 제작 목록 (우선순위 순)

| 우선순위 | 에셋 | 용도 | 권장 크기 |
|---|---|---|---|
| ⭐⭐⭐ | 마스코트 로봇 (기본/기쁨/어지러움/놀람 4종 표정) | 타이틀, 2·3차시 로봇 | 512×512 투명 PNG |
| ⭐⭐⭐ | 앱 아이콘 | 홈 화면 (현재 자동 생성본 있음) | 512×512 PNG |
| ⭐⭐ | 12차시 배지 일러스트 12종 | 배지 화면, 수료증 | 256×256 투명 PNG |
| ⭐⭐ | 고양이/강아지 사진 카드 각 5종 | 2차시 훈련소 | 256×256 |
| ⭐ | 개인정보 카드 아이콘 13종 | 5차시 | 256×256 |
| ⭐ | 자율주행차, 장애물 5종 | 11차시 | 256×256 투명 PNG |
| ⭐ | 타이틀 로고 이미지 | 타이틀 화면 | 1024×512 투명 PNG |

## 3. 생성 프롬프트 예시

> "초등학생용 교육 앱에 쓸 귀여운 파스텔톤 로봇 캐릭터. 둥근 몸, 큰 눈, 하늘색과 노란색, 흰 배경 없이 투명 배경, 플랫 벡터 스타일, 카와이"

스타일 통일을 위해 **같은 대화에서 이어서** 생성하고,
"같은 스타일로 ~를 그려줘"라고 요청하는 것을 추천합니다.

## 4. 파일 넣는 곳

```
assets/
  icons/        앱 아이콘 (icon-192.png, icon-512.png)
  characters/   캐릭터 이미지 (새로 만들 폴더)
  badges/       배지 이미지 (새로 만들 폴더)
```

이미지를 추가한 뒤 `sw.js`의 `CORE` 목록에 경로를 추가하면 오프라인에서도 보입니다.
